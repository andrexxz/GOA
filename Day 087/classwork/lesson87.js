// 1-დან 10-მდე ციფრების გამოშვება
for (let i = 1; i <= 10; i++) {
    console.log(i);
}

// 0-დან 20-მდე ლუწ და კენტ რიცხვებს გამოვიყენებთ
for (let i = 0; i <= 20; i++) {
    if (i % 2 === 0) {
        console.log(`${i} ლუწია`);
    } else {
        console.log(`${i} კენტია`);
    }
}
